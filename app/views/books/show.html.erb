<%= render "users/user_info", user: @user %>
<%= render "books/new", book: @book %>

<p id="notice">
  <%= notice %>
</p>

<h2>Book detail</h2>
<table class="table">
  <tbody>
    <td>img</td>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
    <tb class="btn"><%= link_to "Edit" %></tb>
    <tb class="btn"><%= link_to "Destroy", book_path(@book), method: :delete, "data-confirm" => "本当に消しますか？" %></tb>
  </tbody>
</table>

