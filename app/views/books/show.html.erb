
<%= render "users/user_info", user: @user %>
<%= render "books/new", book: @book %>



<h2>Book detail</h2>
<table class="table">
  <tbody>
    <td>img</td>
    <td><%= @book2.title %></td>
    <td><%= @book2.body %></td>
    <% if @user.id == current_user.id %>
     <tb class="btn"><%= link_to "Edit", edit_book_path %></tb>
     <tb class="btn"><%= link_to "Destroy", book_path(@book2), method: :delete, "data-confirm" => "本当に消しますか？" %></tb>
    <% end %>
  </tbody>
</table>

