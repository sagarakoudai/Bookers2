<h2>User info</h2>
<p>img</p>

<table>
  <tbody>
    <tr>
      <th>name</th>
      <th><%= @user.name %></th>
    </tr>
    <tr>
      <th>introduction</th>
    </tr>
  </tbody>
</table>

<% if @user.id == current_user.id %>
 <%= link_to "edit", edit_user_path(@user) %>
<% end %>